[build]
  command = "npm run build"
  # "public" pasa el check del plugin (≠ base) y evita que se use .next como raíz (que rompe /_next/).
  publish = "public"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"
